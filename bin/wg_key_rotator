#!/usr/bin/env sh
set -eu

script_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
app_dir="$(CDPATH= cd -- "$script_dir/.." && pwd)"

if [ -f "$app_dir/.env" ]; then
  set -a
  . "$app_dir/.env"
  set +a
fi

cd "$app_dir"
exec mix run -e "WgKeyRotator.CLI.main(System.argv())" -- "$@"
